import React from "react";

export default function Col(props){
    //if (props.row_name = "th") {return(<th>props.value</th>)} else {return(<td>props.value</td>)};
   return(<props.row_name>{props.value}</props.row_name>)
}